name: Release

on:
  release:
    types:
      - published

jobs:
  publish:
    name: Publish release
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/publish.yaml
